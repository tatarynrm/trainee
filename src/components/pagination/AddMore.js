import React from "react";

const AddMore = ({ block, currentPage, setCurrentPage }) => {
  return (
    <div>
      <button className="btn btn-primary">Добавити більше</button>
    </div>
  );
};

export default AddMore;
